# 바뀐 내역

버전 번호는 `코드로넘기기.ahk` 맨 위의 `appVersion` 과 같습니다.

## v1.6.4 — 2026-09-25

### 바뀐 것
- **새 아이콘.** AutoHotkey 기본 아이콘 대신, 보라·파랑 바탕에 학사모와 반짝이를 넣은 아이콘(`icon.ico`)입니다. exe 에 넣어 빌드하고, `.ahk` 로 켤 때도 트레이·창 아이콘으로 씁니다.
- **문제풀이 중 오버레이는 노란색.** 알아서 푸는 중이든 직접 풀어야 하든 노란 바탕에 📝 로 바뀝니다. 크게는 셋째 줄에 '퀴즈 푸는 중' / '퀴즈가 나왔습니다', 작게는 '자동 풀이' / '퀴즈'. (전에는 직접 풀 때 빨강, 알아서 풀 때는 평소 색이었습니다)
- **다 들으면:** 오버레이가 초록 완료 카드(`🎓 6/6차시 · 학습을 모두 마쳤습니다`)로 바뀌고 두 번 은은하게 밝아졌다 돌아옵니다(창 투명도만 바꿔 부드럽습니다). 다시 시작할 때까지 초록으로 남습니다.
- **다 들으면 알림 창** ([설정] → 알림, 기본 켜짐). 🎓 과 함께 무엇을 마쳤는지 보여 줍니다.
- **사람이 봐야 할 때 알리기** ([설정] → 알림, 둘 다 기본 켜짐). 모든 돌발 퀴즈를 알아보고 풀 수는 없으므로,
  - 직접 풀어야 할 퀴즈(자동 풀이가 꺼져 있거나 자동으로 못 푼 것)
  - **2분 동안 진행이 멈춤** (시간·페이지·차시·강의 창 제목이 그대로. 알아보지 못한 돌발 퀴즈, 안내창, 가려져 멈춘 영상 등)

  이면 오버레이가 **빨갛게 진해졌다 연해졌다** 하고(창 투명도만 바꿔 부드럽다), **알림 창**이 뜹니다. 알림 창은 앞으로 튀어나오지 않게 뜨고, [강의 창 보기] 로 밀어 둔(F11) 창도 꺼내 줍니다. 다시 진행되면 저절로 닫힙니다. 빨강을 끄면 노랑으로만 표시합니다.
- 차시 목록 창이 **최소화**돼 있으면 시작할 때·[목록 읽기]·차시를 넘길 때 앞으로 가져오지 않고 잠깐 펼쳐 읽고 누른 뒤 다시 내립니다. 최소화된 브라우저 창은 내용을 비워 두기도 해서(한국보건복지인재원 나의학습실: 링크 0개로 읽힘) 그냥은 못 읽습니다. 1분마다 다시 찾을 때는 펼치지 않습니다.
- **브라우저 알림창 닫기.** 영상이 멈춰 있을 때 [확인] 하나뿐인 알림창('학습이 종료되어야 다음 페이지로의 이동이 가능합니다.' 등)이 떠 있으면 닫습니다. [확인]·[취소] 가 같이 있는 창은 누르지 않고 알립니다.

### 고친 것
- **한국보건복지인재원: 차시 제목에 '문제' 가 들어가면 퀴즈로 알아보던 문제.** 5차시 '…인권문제 원인과 대안' 을 퀴즈로 보고, 재생을 누르지 않거나 영상이 끝나기 전에 다음 페이지를 눌러 사이트 알림창이 떴습니다. 퀴즈 낱말을 이 사이트 퀴즈에만 나오는 말(`옳으면 O`, `틀리면 X`, `정답을 고르세요`)로 바꿨습니다.

## v1.6.3 — 2026-09-25

### 고친 것
- **단축키(F9·F11·F10)가 지워지던 문제.** 한 번도 열지 않은 설정 창이 닫히면(밖에서 보낸 닫기 신호 등) 빈 단축키 칸을 그대로 저장했습니다. 그 뒤로 F11 이 먹지 않아, F11 로 되돌리려 해도 창이 숨은 채로 남았습니다. 이제 연 적 없는 설정 창이 닫히면 아무것도 바꾸지 않습니다.
- F11 을 조금 길게 눌러 두 번 들어가면 숨겼다 바로 되돌리던 문제 (0.7초 안의 두 번째는 무시). 켬/끔을 상태 칸에 분명히 적습니다.
- **한국보건복지인재원 나의학습실 창이 넓으면 차시 표를 못 읽던 문제.** 이 페이지는 창이 좁으면 칸이 위아래로 쌓이고, 넓으면 가로 표가 됩니다. 가로 표의 번호 칸은 `3차시` 로 적혀 있는데 숫자만 있는 칸(`3`)만 번호로 알아봐서 한 줄도 못 읽었습니다. 이제 `3차시` 도 번호로 보고, `28분 10초` 같은 학습시간 칸은 이름으로 잘못 잡지 않습니다.

### 바뀐 것
- **크게 오버레이:** 글자를 줄이고 지금 듣는 강의를 글로 보여 줍니다. 윗줄 `📖 3/6차시 11/13p   ▶ 01:40/02:30`, 둘째 줄 차시 이름, 셋째 줄 페이지 이름(알릴 말이 있으면 그것). 프로필 `[읽을것] 영상제목` 으로 어디서 읽을지 정합니다.
- 차시 표 창을 켤 때 못 찾았으면 진행 중 1분마다 다시 찾습니다. 최소화된 목록 창도 쓰고, 같은 창이 여럿이면 보이는 창을 먼저 씁니다.

## v1.6.2 — 2026-09-25

### 고친 것
- **밀어 둔(F11) 강의 창이 페이지가 넘어갈 때마다 다시 나오던 문제.** 한국보건복지인재원 학습창은 페이지마다 사이트가 창 자리를 다시 잡습니다. 이제 밀어 둔 동안 1초마다 창 자리만 보고, 다시 나와 있으면 도로 밀어 둡니다. 차시가 바뀌어 새 강의 창이 열려도 밀어 둡니다. 꺼내려면 F11.

### 실제로 해 본 결과
- v1.6.0 exe(릴리스에서 받은 것)를 켜니 v1.6.1 을 알려 주고, [예] → 받기·SHA256 확인 → exe 교체 → 새 exe 로 다시 켜짐 → `.old` 지워짐. 고친 프로필은 그대로 + `.new`, 개인 설정 그대로.
- 밀어두기 지키기: 시험 창을 밀어 둔 뒤 다른 자리로 옮기면 1.6초 안에 도로 밀림. 새 창도 밀림. F11 로 되돌리면 지키기 꺼짐.

## v1.6.1 — 2026-09-25

### 고친 것
- **exe 판 업데이트 확인이 실패하던 문제.** 릴리스 설명이 3,000자쯤 넘으면 설명을 읽는 정규식이 한도(PCRE -21)에 걸려 오류가 났고, 업데이트 확인이 소리 없이 끝났습니다. v1.6.0 설명이 3,406자라 실제로 걸렸습니다. 이제 한 글자씩 읽어 길이 한도가 없습니다.
- 배포 스크립트가 릴리스 설명을 짧게(JSON 2,500자 안) 만듭니다. v1.6.0 exe 는 옛 코드라 짧은 설명만 읽을 수 있어서, 그래야 v1.6.0 에서 이 버전으로 스스로 넘어올 수 있습니다. 전체 내역은 이 파일에 둡니다.

## v1.6.0 — 2026-09-25

### 새로 생긴 것
- **exe 판 업데이트 (GitHub Releases).** exe 로 쓰면 켤 때 최신 릴리스를 보고, [예] 를 누르면 zip 을 받아 SHA256 을 확인한 뒤 exe 를 스스로 바꾸고 다시 켭니다. 실행 중인 exe 는 이름을 `.old` 로 바꿔 두고 다음에 켤 때 지웁니다. 사이트 프로필은 새 것만 더하고, 손대지 않은 것만 바꾸고, 직접 고친 것은 그대로 두고 옆에 `.new` 로 둡니다(`사이트\원본.txt` 로 구분). 설치·레지스트리 없음. `.ahk` + git 은 전처럼 `git pull`.
- **배포 스크립트 `release.ps1`.** 검사 → exe 빌드 → zip(exe·사이트 프로필·원본 목록·설명서) → `SHA256SUMS.txt` → CHANGELOG 로 릴리스 설명. `-Publish` 를 붙이면 태그 push 와 GitHub 릴리스까지 합니다(`gh` 필요). exe 는 저장소에 커밋하지 않습니다.
- **한국보건복지인재원 (edu.kohi.or.kr) 프로필.** `사이트/한국보건복지인재원.ini`. plyr 플레이어 학습창과 '나의학습실' 차시 표를 모두 읽습니다.
- **교육 페이지 열기 버튼 🌐.** 본체 창 사이트 칸 왼쪽. 프로필 `[사이트] 주소` 를 기본 브라우저로 엽니다. 세 프로필 모두 주소를 적어 두었습니다. 프로필은 직접 고치는 파일이라 `http://`·`https://` 주소만 열고, 파일·프로그램·`javascript:` 는 막습니다.
- **`안쪽:` 읽기 규칙.** `현재시간 = 안쪽:class:plyr__time--current` 처럼 쓰면 요소 이름('Current time')을 버리고 안의 글자('00:11')를 읽습니다.
- **칸이 위아래로 쌓인 차시 표.** `차시링크 = 버튼:...` 이 같은 줄에서 번호를 못 찾으면, 버튼 위쪽의 가장 가까운 'N차시' 칸부터 버튼까지를 한 차시로 봅니다. 이름('차시 명 …')·진도율('진도율(%) 7%')·학습시간도 그 칸들에서 읽습니다.

### 바뀐 것
- **프로필을 고를 때 더 자세한 창제목이 이깁니다.** 한국보건복지인재원 학습창 제목에 '학습창' 도 들어 있어 지방자치인재개발원 프로필이 잡혔습니다. 이제 창제목이 더 긴(자세한) 프로필을 고르고, 지난번에 쓰던 프로필보다 지금 열린 창에 더 맞는 프로필이 있으면 켤 때 그쪽으로 바꾸고 상태 칸에 알립니다.
- 차시 목록에 진도율을 함께 보여 줍니다 (`1차시  남음   7%  11초  인권의 이해`).
- **`[읽을것] 차시 = 제목:창제목`.** 학습창에 차시 번호가 없는 사이트는 창 제목에 든 차시 이름('인권의 이해 > OUTRO < 학습창')을 차시 표에서 찾아 번호로 바꿉니다. 번호를 알아야 '다음 번호' 로 갑니다. 모르면 진도율이 100 이 아닌 첫 차시로 가는데, 방금 들은 차시가 닫기 전이라 92% 로 보여 같은 차시를 또 열 뻔했습니다.
- 본체 창 '차시 목록' 칸의 첫 줄이 어느 창을 찾는지 보여 줍니다 (`(자동으로 찾기 : 나의학습실 창)`). 고른 창에 차시 표가 없으면 바로 알려 줍니다.
- 다음 차시로 넘어갈 때 방금 들은 차시의 진도율이 100% 가 아니면 알립니다 (넘어가기는 합니다).

### 고친 것
- **'차시 목록' 에 표가 없는 창을 고르면, 차시가 끝났을 때 '모든 차시를 마쳤습니다' 로 멈추던 문제.** 차시 표를 한 줄도 못 읽었는데 다 마친 것으로 봤습니다. 이제 고른 창에 표가 없으면 프로필에 적힌 창(나의학습실)으로 다시 찾고, 거기서도 못 읽으면 '표를 읽지 못했다' 고 알립니다.
- **'학습시간 0초' 를 들은 차시로 보던 문제.** 전에는 `00:00:00` 모양만 안 들은 것으로 봤습니다. 이제 진도율을 읽을 수 있으면 진도율(100% = 다 들음)로, 없으면 학습시간에 0 이 아닌 숫자가 있는지로 판단합니다.

### 실제로 해 본 결과
- 한국보건복지인재원 '사회복지시설종사자의인권' (6차시, 1차시 14페이지):
  - 학습창: 시간 00:11/00:11 · 페이지 1/14 · 재생/멈춤 모두 읽힘. 재생 버튼을 코드로 눌러 00:02 → 00:07 로 흐르는 것 확인.
  - 차시 표: 6차시 이름·진도율 모두 읽힘.
  - 차시 이동: 학습창 닫기 → 표 다시 읽기 (1차시 0% → 7%, 닫아야 갱신됨) → '학습하기' → 새 학습창이 이어보기 위치(2/14 페이지)에서 멈춘 채로 열림. 안내창은 뜨지 않았습니다.
  - 켤 때 프로필이 한국보건복지인재원으로 저절로 바뀌고 00:00/02:08, 2/14 로 읽힘.
  - 1차시 14페이지를 코드로 넘겨 끝까지 (넘김 12회). 끝난 뒤 표는 92% → 학습창을 닫으니 100% (28분 10초).
  - 1차시 14/14 → 학습창 닫기 → 2차시 '학습하기' → 2차시 INTRO 재생 → 2/12 페이지 재생 중. 차시 번호도 창 제목으로 2 로 읽힘.
- exe 업데이트 (따로 시험):
  - `release.ps1` 로 빌드한 zip 을 윈도우 tar 로 풀면 한글 파일 이름이 그대로 나오고, 풀린 exe 가 빌드한 exe 와 SHA256 이 같음.
  - 프로그램의 SHA256 계산이 PowerShell `Get-FileHash` 와 같음. 릴리스 JSON 읽기(따옴표·역슬래시·`\u` 포함), 버전 비교(1.10.0 > 1.9.2) 통과.
  - 프로필 바꾸기: 손대지 않은 것 → 새 판, 고친 것 → 그대로 + `.new`, 없던 것 → 더함.
  - 실행 중인 exe 이름 바꾸기 → 새 exe 넣기 → 새 exe 켜기 → 새 exe 가 `.old` 지우기를 작은 exe 로 확인.
  - GitHub API: 저장소 공개 확인. 릴리스가 아직 없어 `releases/latest` 는 404 (정상).
- 아직 못 본 것: 이 사이트의 퀴즈 화면, 실제 릴리스에서 받아 바꾸기(다음 버전이 나와야 해 볼 수 있음).

## v1.5.0 — 2026-09-24

### 새로 생긴 것
- **진행 중에는 강의 창을 항상 위에** ([설정] → 시스템, 기본 켜짐). 다른 프로그램을 전체 화면으로 써도 강의 창이 완전히 덮이지 않아 브라우저가 영상을 멈추지 않습니다. 정지·종료하면 풉니다. 강의 창이 바뀌면 새 창으로 옮기고, 상태 칸 '창' 줄에 '· 항상 위' 로 나옵니다.
  - 원래부터 항상 위였던 창(PowerToys 등으로 직접 켜 둔 것)은 끝날 때도 그대로 둡니다. 시험하다 사용자가 켜 둔 항상 위를 풀어 버린 일이 있어서, 프로그램이 켠 경우에만 풀도록 했습니다.
  - 밀어둔 창(F11)은 밀어두기가 따로 관리합니다.

### 실제로 해 본 결과 (이어서)
- v1.4.3 으로 3차시 마지막 페이지 → 영상 창 닫기 → 4차시 `학습하기` 자동으로 넘어감. 1·2·3차시 진도율 100%.
- v1.5.0 으로 4차시 8페이지(5페이지 O/X 퀴즈 포함, 30분짜리 6페이지 포함) → 5차시(아웃트로) → '모든 차시를 마쳤습니다' 로 멈춤 (23:14).
- **전기안전작업 관계자 교육 1~5차시 모두 진도율 100%.** 끝난 직후 표는 5차시가 0% 로 보였지만, 영상 창이 열려 있어 갱신이 안 된 것이었고 '진도율 새로고침' 을 누르니 100% 로 나왔습니다.
- 5차시는 앞 차시를 다 듣기 전에는 '학습동영상이 등록되지 않았습니다.' 가 뜹니다. 앞 차시를 모두 마치면 열립니다.

## v1.4.3 — 2026-09-24

### 새로 생긴 것
- **오류 기록.** 오류가 나면 `코드로넘기기_기록.txt` 에 시각·버전·오류·줄·호출 경로를 남깁니다. 오류 창은 닫으면 내용이 사라져서 무엇이 문제였는지 알 수 없었습니다. 이제 오류 창을 띄우지 않고 기록만 남기며, 같은 오류는 한 번만 소리·텔레그램으로 알립니다. 프로그램은 멈추지 않고 다음 차례에 이어서 돕니다. (GitHub 에는 올리지 않음)

### 고친 것
- **차시를 넘길 때 오류로 멈추던 문제** (v1.3.3 부터). 영상 창을 닫은 뒤 차시 표를 다시 읽는 곳에서, 없는 속성(`target.fresh`)을 읽어 `PropertyError` 가 났습니다.
- **차시가 바뀔 때 '영상이 끝나 처음으로 되돌아감' 표시가 안 지워지던 문제.** 비우는 함수가 전역 변수를 가리키지 않았습니다(`videoWrapped`). AutoHotkey 경고 검사(`#Warn`)로 찾았습니다.

## v1.4.2 — 2026-09-24

### 바뀐 것
- **작게 오버레이에도 시간 표시.** 두 줄이 되었습니다. 윗줄 `▶ 3/5차시 5/9p`, 아랫줄 작은 글씨 `02:31/17:43`. 크게와 마찬가지로 화면을 읽지 않는 동안에도 시계가 혼자 흐릅니다.
- 전체 시간이 안 나오는 플레이어(산업안전포털 인트로 등)는 `--:--/--:--` 대신 지금 시간만 보여 줍니다.

## v1.4.1 — 2026-09-24

### 정리
- 예전 프로그램 파일을 저장소와 기록에서 모두 뺐습니다. 관련 설명도 지웠습니다.
- 예전 설정 파일에서 텔레그램 정보를 가져오던 코드를 뺐습니다. 텔레그램은 [설정] → 알림 에 적습니다.

## v1.4.0 — 2026-09-24

### 새로 생긴 것
- **퀴즈를 틀리면 다른 답을 누릅니다.** 답을 누르고 기다린 뒤, 누른 답에 오답 표시가 붙었으면 아직 안 누른 다른 답을 누릅니다. O/X 는 O 가 틀리면 X, 4지선다는 다음 보기 순서입니다. 모든 답을 눌러 봤으면 그냥 넘깁니다.
  - 까닭: 산업안전포털 2차시에서 O/X 퀴즈를 틀린 채(`quizCheck_btn Li_1 check wrong`) 넘겼더니 8페이지 중 7페이지만 인정되어 진도율이 88% 에 머물렀습니다.
  - 오답 표시는 프로필 `[문제풀이] 오답표시` 에 정규식으로 적습니다. 비워 두면 `\bwrong\b|incorrect|오답|틀렸` 입니다.
  - 누른 답은 '이름 + 화면 자리' 로 기억합니다. 누른 뒤 class 가 바뀌어도 같은 답으로 알아봅니다.
  - 오답이라 다시 누르는 것은 '두 번 해도 안 넘어가면 사람에게 알림' 횟수에 세지 않습니다.

### 실제로 해 본 결과 (이어서)
- v1.3.2 로 3차시 → 4차시, 4차시 → 5차시(아웃트로)로 넘어감. 5차시는 사이트에 '학습동영상이 등록되지 않았습니다.' 가 떠서 처음 보는 안내창으로 알리고 멈춤 (사이트 쪽 문제로 보임)

## v1.3.3 — 2026-09-24

### 고친 것
- **차시가 끝나도 다음 차시로 넘어가지 않던 문제** (산업안전포털). 다음 차시로 가기 전에 영상 창을 닫는데, 닫기 규칙 `class:close && name:닫기` 가 영상 창의 X 대신 페이지 구석의 2픽셀짜리 '메뉴 닫기'(`closeMenu`)를 먼저 잡았습니다. 영상 창이 안 닫힌 채 `학습하기` 를 눌러 아무 일도 일어나지 않았습니다.
  - 프로필 규칙에 **정확히 같은 것만** 찾는 방법을 추가했습니다. 값을 따옴표로 감싸면 됩니다. 예) `class:"close"` 는 class 가 정확히 `close` 인 것만 찾고, `closeMenu`·`TabCloseButton` 은 빼냅니다.
  - 산업안전포털 프로필의 닫기를 `class:"close" && name:닫기` 로 바꿨습니다.
  - 영상 창을 닫은 뒤에는 화면이 새로 그려지므로, 차시 표를 다시 읽어 같은 번호의 `학습하기` 를 누릅니다. 닫기를 못 누르면 알립니다.
- **브라우저 자체의 버튼을 누를 수 있던 위험.** 같은 닫기 규칙이 브라우저 **탭의 닫기 버튼**(`TabCloseButton` / 닫기)에도 맞았습니다. 이번에는 순서상 '메뉴 닫기' 가 먼저 잡혀 괜찮았지만, 강의 탭을 닫을 수도 있었습니다. 이제 어떤 규칙이든 웹페이지(문서) 안의 요소만 찾습니다. 탭 줄·주소창·창 버튼은 잡지 않습니다.

## v1.3.2 — 2026-09-24

### 고친 것
- **프로그램을 다시 켜면 오버레이·상태 칸에 차시가 안 나오던 문제** (`4/9p` 만 보이고 `차시 : ?`). 산업안전포털 차시 표에서 이미 들은 줄에는 '최초·최종 학습일시' 칸(`2026.09.24 17:38:01`)이 생깁니다. 이 칸이 차시 이름보다 길어서 이름으로 잘못 뽑혔고, 그래서 영상 제목 '전기기계·기구의 안전' 과 맞는 줄을 못 찾았습니다. 이제 날짜·시간·숫자로만 된 칸은 이름으로 치지 않습니다.
  - 이 상태로 차시가 끝났다면 '다 듣지 않은 첫 줄' 을 골라 지금 차시를 처음부터 다시 틀 수 있었습니다. 이제는 지금 차시 번호를 알고 다음 차시로 갑니다.

## v1.3.1 — 2026-09-24

### 고친 것
- **강의 창에서 다른 탭을 열면 엉뚱한 페이지를 읽던 문제.** 강의가 열린 브라우저 창에서 GitHub 탭을 열었더니, 강의를 못 읽고 GitHub 페이지의 `7 / 350 characters` 를 페이지 번호 `5/350` 으로 읽었습니다. 이 프로그램은 창에서 '지금 보이는 탭' 만 읽을 수 있습니다.
  - 이제 창 제목에 사이트 이름(프로필 `[사이트] 창제목`)이 없으면 다른 탭을 보고 있는 것으로 봅니다. 그동안은 아무것도 읽거나 누르지 않습니다.
  - 상태 칸에 지금 보이는 탭을 알려 주고, 오버레이는 '⚠ 탭 바뀜' 으로 빨개집니다. 30초 넘게 이어지면 소리·텔레그램으로 알립니다.
  - 창을 직접 고른 경우는 제목이 프로필과 다를 수 있으므로 따지지 않습니다.
  - 다른 사이트는 **새 창(Ctrl+N)** 에서 열면 강의에 영향이 없습니다.

### 실제로 해 본 결과 (이어서)
- 2차시 7·8페이지 → **3차시로 자동으로 넘어감** (18:35), 3차시 1→2→3페이지 자동 넘김

## v1.3.0 — 2026-09-24

### 새로 생긴 것
- **GitHub 버전관리와 업데이트 확인.** 켤 때 새 버전이 있으면 바뀐 내용을 보여 주고 받을지 묻습니다. [설정] → [업데이트 확인] 으로 직접 확인할 수도 있습니다. `git pull --ff-only` 로만 받아서, 이 PC 에서 고친 파일과 겹치면 덮어쓰지 않고 까닭을 보여 줍니다.
- **오버레이에 차시 표시.** 위쪽 줄이 `7/8` 대신 `2/5차시 7/8p` 로 나옵니다. 페이지가 없는 영상은 `1/5차시`, 차시를 모르면 `7/8p`.
- 본체 창 머리글과 설정 창에 지금 버전을 표시합니다.

### 고친 것
- **재생 중인데 오버레이가 ❚❚(멈춤)으로 보이던 문제.** 창이 가려지거나 뒤에 있으면 브라우저가 플레이어의 시간 글자를 1분에 한 번쯤만 바꿉니다. 그래서 시간이 10초 넘게 그대로면 멈춤으로 표시했습니다. 이제 재생 상태를 알려 주는 플레이어(산업안전포털)는 그 표시를 믿습니다. 상태 칸에는 "시간 표시만 N초째 그대로 · 창이 가려지면 늦게 바뀜" 으로 나옵니다. 재생 버튼을 누를지는 원래 재생 표시로 판단했으므로, 영상을 잘못 누른 일은 없었습니다.

## v1.2.0 — 2026-09-24 · 산업안전포털 새 플레이어

산업안전포털 강의실 페이지 안에 바로 뜨는 영상 창(스킨3, video.js)을 지원합니다.

### 새로 생긴 것
- **사이트 안내창 자동 처리** (프로필 `[안내창]`)
  - "1페이지 동영상 학습이 완료되었습니다. 다음 동영상 페이지를 학습하시겠습니까?" → `확인` 을 누릅니다.
  - "종료·나가기·취소·삭제·재수강·응시" 가 들어간 안내창은 누르지 않고 알립니다.
  - 처음 보는 안내창은 누르지 않고 내용을 소리·텔레그램으로 한 번 알립니다.
  - **영상이 재생 중이면 누르지 않습니다.** 확인을 눌러 다음 영상이 떠도 지난 안내창이 화면에 남아 있는 경우가 있습니다. 그걸 또 누르면 차시를 하나씩 더 건너뛰기 때문입니다. 시간이 끝까지 갔으면 재생 표시가 남아 있어도 끝난 것으로 봅니다.
- **전체 시간이 없는 플레이어의 끝 알아보기.** 프로필 `[상태] 끝남 = class:vjs-ended`. '끝남' 표시만 보고 판단할 때는 8초 기다립니다. 끝나고 조금 뒤에 안내창이 뜨기 때문입니다.
- **현재 시간만 있는 시간 표시** 읽기 (`00:01:47`).
- **버튼으로 된 차시 표.** `차시링크 = 버튼:class:takeLesson` 을 쓰면, 줄마다 있는 `학습하기` 버튼을 찾습니다. 같은 줄의 맨 왼쪽 숫자를 차시 번호, `40%` 를 진도율, 가장 긴 글을 차시 이름으로 읽습니다. 표 칸이 있는 줄은 표 칸만 봅니다. 같은 높이에 뜬 안내창 글이 섞이지 않게 하기 위해서입니다.
- **제목으로 지금 차시 알아보기.** `[읽을것] 차시 = 제목:class:eduVideo` 는 영상 창 제목이 차시 표의 어느 줄 이름으로 시작하는지로 번호를 찾습니다. 제목에 번호가 없는 사이트용입니다.
- **차시를 넘기기 전에 누를 것.** `[차시] 넘기기전 = 닫기` 이면 영상 창을 닫고 다음 줄의 `학습하기` 를 누릅니다.
- **차시를 넘긴 직후 보호.** 새 영상이 실제로 도는 것을 볼 때까지(최대 2분) '끝남' 을 믿지 않습니다. 새 강의가 늦게 뜨면 지난 영상의 '끝남' 이 그대로 보여 한 차시를 더 건너뛸 수 있기 때문입니다.

### 실제로 해 본 결과 (2026-09-24, 전기안전작업 관계자 교육)
- 인트로(1페이지) 끝 → 안내창 확인 → 2차시로 넘어감
- 2차시 8페이지: 1→2→3→4→5페이지 자동 넘김, 6페이지 O/X 퀴즈 자동 통과, 7페이지 진행 확인

## v1.1.0 — 2026-09-24 · 지방자치인재개발원 차시 넘김 고침

### 고친 것
- **새 차시를 조금 보다가 또 다음 차시로 넘어가던 문제.** 차시를 누른 뒤에도 기다리는 동안 화면을 읽었는데, 그때 지난 차시의 끝 시간(예: 30:00)이 저장됐습니다. 새 영상이 00:05 / 36:26 으로 시작하면 "끝에서 0으로 되돌아감(존플레이어의 끝)" 으로 잘못 알아봤습니다.
  - 전체 시간이 바뀌면 다른 영상으로 보고 지난 시간을 버립니다.
  - 그 영상에서 시간이 흐르는 것을 한 번이라도 본 뒤에만 '되돌아감' 을 끝으로 봅니다.
- **차시 목록 읽기에서 멈추던 오류** (`This local variable has not been assigned a value. Specifically: p`). 함수 안의 변수 `p` 가 프로필을 읽는 함수 `P()` 를 가렸습니다. AutoHotkey 는 대소문자를 구분하지 않기 때문입니다.

### 새로 생긴 것
- **다 들은 차시는 건너뜀.** 학습창 차시 목록의 `class="step p100"`(다 들음) / `step p0` 을 읽습니다. 프로필 `[차시] 진도율 = class무늬:\bstep\b.*\bp(\d+)\b`. 다음 차시를 고를 때 진도율 100 인 차시를 건너뛰고, 상태 칸과 텔레그램에 "(다 들은 6차시는 건너뜀)" 으로 알립니다.
  - 학습현황에 시간이 조금이라도 있으면 '들음' 으로 보던 예전 기준은 쓰지 않습니다. 버그로 잠깐 재생된 차시(00:00:13)가 들은 것으로 잘못 잡혔기 때문입니다.
- **상태 칸에 화면 읽는 주기 표시.** `시간 : 01:29 / 53:49   (5초마다 읽음)`. 본체 창을 숨겨 두어 더 드물게 읽을 때는 ", 지난번은 40초 전" 도 붙습니다.
- **본체 창에 "차시가 끝나면 다음 차시로" 체크박스.** 설정 창의 같은 항목과 함께 움직입니다. 상태 칸에 `다음차시 : 자동 (켬) · 다 들은 차시 2·6 건너뜀` 줄이 생겼습니다.

## v1.0.0 — 2026-09-23 까지

화면 좌표·색을 보는 대신 웹페이지 코드로 버튼을 누르는 방식으로 만든 첫 판입니다.

- 사이트별 프로필 (`사이트/*.ini`), 산업안전포털(스킨 2가지)·지방자치인재개발원(플레이어 2가지)
- 영상이 끝나면 다음, 멈추면 재생, 마지막 페이지에서 알림·닫기
- 퀴즈 알림, 퀴즈 아무 답이나 누르고 넘기기, 안 되면 차례로 눌러 보기
- 차시가 끝나면 다음 차시로, 어디까지 들을지, 차시 목록 읽기
- 작업 중 오버레이 (크게/작게/숨김), 창 밀어두기 (F11), 단축키 바꾸기
- 텔레그램 알림, 잠들지 않게, 브라우저 창을 앞으로 가져오지 않기
- 화면 읽는 주기, 영상이 도는 동안에는 끝날 때까지 쉬기
- 코드 목록 뽑기 (F10) 로 새 사이트 프로필 만들기
